# Supabase Configuration (Required)
# Get these from your Supabase project settings: https://app.supabase.com
PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co
PUBLIC_SUPABASE_PUBLISHABLE_KEY=your-anon-public-key-here

# Supabase Service Role Key (CRITICAL - KEEP SECRET!)
# WARNING: This key bypasses ALL Row Level Security policies
# - ONLY use server-side (never in client code)
# - NEVER commit to git or share publicly
# - If leaked, regenerate immediately in Supabase dashboard
# - Only needed for privileged operations (payment processing, admin actions)
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# Stripe Configuration (Required for payments)
# Get these from: https://dashboard.stripe.com/apikeys
STRIPE_SECRET_KEY=sk_test_your_secret_key_here
PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_your_publishable_key_here

# Stripe Webhook Secret (Required for payment confirmation)
# Get this from: https://dashboard.stripe.com/webhooks
# After setting up webhook endpoint: /api/stripe/webhook
STRIPE_WEBHOOK_SECRET=whsec_your_webhook_secret_here

# Node Environment
NODE_ENV=development

# ============================================================================
# PRODUCTION ONLY (Optional for development)
# ============================================================================

# Distributed Rate Limiting (Required for production with multiple servers)
# Sign up at https://upstash.com for free Redis
# UPSTASH_REDIS_REST_URL=https://your-redis-url.upstash.io
# UPSTASH_REDIS_REST_TOKEN=your-redis-token

# ============================================================================
# Notes:
# ============================================================================
#
# 1. Copy this file to .env and fill in your actual values
# 2. Never commit .env to version control
# 3. The PUBLIC_ prefix exposes variables to the client-side
# 4. Keep your SUPABASE_PUBLISHABLE_KEY public - it's safe to expose
# 5. For production deployments, see PRODUCTION_NOTES.md
#
# ============================================================================
